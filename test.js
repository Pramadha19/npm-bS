const {
    binarySearch,
    
  } = require('./index'); 
  // Test data
  const array = [1, 2, 3, 4, 5];
  const target = 3;
  
  //  binarySearch
  console.log('Binary Search:', binarySearch(array, target)); // Should return 2
  
  